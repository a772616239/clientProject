package model.mistforest.room.entity.MistMazeRoom;

import model.mistforest.room.entity.MistRoom;

public class MistMazeRoom extends MistRoom {
    public MistMazeRoom() {
    }

    public void refreshMazeRoom() {
//        MistPlayer player;
//        for (String memberIdx : memberList) {
//            player = MistPlayerCache.getInstance().queryObject(memberIdx);
//            if (player == null) {
//                continue;
//            }
//            CS_GS_MineFightRecord.Builder builder = CS_GS_MineFightRecord.newBuilder();
//            builder.setPlayerIdx(memberIdx);
//            broadcastMsg(MsgIdEnum.CS_GS_MineFightRecord_VALUE,);
//            GlobalData.getInstance().sendMsgToServer(player.getFromSvrIp(), )
//        }
    }
}
